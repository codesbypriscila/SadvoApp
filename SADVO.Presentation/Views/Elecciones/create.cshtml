@model SADVO.Application.ViewModels.EleccionViewModel
@{
    ViewData["Title"] = "Nueva Elección";
}
<div class="max-w-2xl mx-auto bg-white rounded-2xl shadow-xl p-8 border border-gray-100 mt-10">
    <h2 class="text-2xl font-bold text-gray-900 mb-6">Nueva Elección</h2>

    <form asp-action="Create" method="post" class="space-y-6">
        <div>
            <label asp-for="Nombre" class="block text-sm font-medium text-gray-700">Nombre de la Elección</label>
            <input asp-for="Nombre" class="mt-2 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-orange-500 focus:border-orange-500" />
            <span asp-validation-for="Nombre" class="text-red-500 text-sm"></span>
        </div>

        <div>
            <label asp-for="FechaRealizacion" class="block text-sm font-medium text-gray-700">Fecha de Realización</label>
            <input asp-for="FechaRealizacion" type="date" class="mt-2 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-2 focus:ring-orange-500 focus:border-orange-500" />
            <span asp-validation-for="FechaRealizacion" class="text-red-500 text-sm"></span>
        </div>

        <div class="flex justify-end gap-4">
            <a asp-action="Index" class="px-5 py-2 bg-gray-200 rounded-md hover:bg-gray-300 transition">Cancelar</a>
            <button type="submit" class="px-5 py-2 bg-orange-600 text-white rounded-md hover:bg-orange-700 transition">Crear</button>
        </div>
    </form>
</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
